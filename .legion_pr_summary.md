Updated `eslint` from v9 to v10 in `package.json`, keeping the existing flat config (`eslint.config.js`) which is already compatible with v10. Fixed a TypeScript compilation error in `__tests__/user.test.ts` where the `updateUser` mutation request was incorrectly typed as `gqlMutation<{ id: string, input: UpdateUserInput }>` — since the `gqlMutation<T>` interface wraps variables under `{ input: T }` but this mutation's variables have `id` and `input` at the top level, the assertion was changed to `gqlRequest`. Both `tsc --noEmit` and `yarn lint` pass cleanly.
