Added an integration test for the `updateUser` mutation in `__tests__/user.test.ts`. The test creates a user, updates only the `displayName` field, then asserts the changed field matches the update input while the untouched `profilePicUrl` remains unchanged and system fields (`id`, `createdDate`, `updatedDate`) are preserved. The branch was rebased onto main (which includes the ESLint v9 upgrade) and all lint checks pass cleanly.
